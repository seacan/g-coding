2.3  Find the length of the smallest contiguous subarray whose sum is greater than or equal to ‘S’.
2.6  Find the length of the longest substring which has no repeating characters.
2.7  Allowed to replace no more than ‘k’ letters with any letter, find the length of the longest substring having the same letters after replacement.
2.13 Find the smallest substring in the given string which has all the characters of the given pattern.
2.15 Find all the starting indices of substrings in the given string that are a concatenation of all the given words exactly once without any overlapping of words.
3.7  Count all triplets in it such that sum < target.
3.9  Given an array containing 0s, 1s and 2s, sort the array in-place.
3.11 Find all unique quadruplets whose sum is equal to the target number.
3.15 Find the length of the smallest subarray in it which when sorted will sort the whole array.